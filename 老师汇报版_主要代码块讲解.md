# 当前主要代码块讲解（汇报版）

这份说明不是逐行讲代码，而是把当前项目里最关键的代码块挑出来，解释三件事：

1. 这段代码做什么；
2. 输入输出是什么；
3. 它在论文整体思路里对应哪一部分。

---

## 一、`run_preliminary_leo_routing.m` 里的关键代码块

### 1. `defaultConfig()`

#### 做什么

这个函数负责统一设置前期仿真的基础参数，包括：

- 星座规模；
- 队列上限；
- 节点服务能力；
- 链路容量；
- 参考时延；
- 队列、负载、风险、寿命、进展这些代价项的权重；
- `tSafe`、`rMin`、`bMinMbps` 这些动作过滤阈值；
- `helloPeriodSlots`、字段字节数、隐藏层规模这些辅助参数。

#### 输入输出

- 输入：无
- 输出：`cfg` 结构体

#### 对应论文思路

它对应文稿里的“仿真参数”和“奖励/动作过滤超参数”。

#### 我的写法思路

我把它集中写在一个函数里，是因为后面做消融和场景对比时，参数不能散在脚本里，否则后面很难统一改。它的角色有点像“实验配置中心”。

---

### 2. `buildTopologyState(...)`

#### 做什么

这个函数负责在每个时间片构造当前网络状态，也就是时变图 `G(t)` 的具体实现。它会生成：

- 链路传播时延 `delay`
- 链路容量 `capacity`
- 已占用速率 `usedRate`
- 负载率 `rho`
- 可靠性 `reliability`
- 中断概率 `pout`
- 剩余可用时间 `trem`
- 邻居状态年龄 `ageTo`
- 可用性 `available`

#### 输入输出

- 输入：当前时间片 `t`、当前队列、当前链路已用速率、AoI、配置 `cfg`
- 输出：`X`，也就是当前时刻的拓扑和链路状态

#### 对应论文思路

它直接对应文稿里的：

`G(t) = (V, E(t), X(t))`

以及 `x_i(t)`、`x_ij(t)` 这些节点和链路状态变量。

#### 我的写法思路

我没有把网络写成静态图，而是每个时间片重建一次状态，是为了明确对应“LEO 网络是高动态拓扑”的前提。后续不管是启发式策略还是 MARL 环境，实际上都依赖这个时变状态构造。

---

### 3. `buildRoutingWeights(...)`

#### 做什么

这个函数负责给不同策略生成路径代价矩阵或动作过滤结果。它会根据策略开关决定：

- 是否加入队列项；
- 是否加入负载项；
- 是否加入可靠性风险项；
- 是否加入方向进展项；
- 是否根据 `T_rem`、`R_min`、`B_min` 做动作 mask。

#### 输入输出

- 输入：拓扑状态 `X`、当前队列、配置 `cfg`、策略配置 `policy`、目的节点 `dstNode`
- 输出：
  - `W`：路径代价或候选边代价
  - `invalidCount`：被过滤的动作数
  - `candidateCount`：候选动作总数

#### 对应论文思路

这部分对应：

- 队列感知；
- 负载感知；
- 链路寿命约束；
- 可靠性惩罚；
- action mask。

#### 我的写法思路

这个函数的作用是把不同策略统一到一个框架下。这样 Dijkstra baseline 和后面的局部策略就不是完全分裂的两套逻辑，而是共用一套状态和代价思想，只是看用了哪些项。

---

### 4. `routeLocalNextHop(...)`

#### 做什么

这个函数是 MATLAB 里真正把“全局路径”改成“逐跳本地决策”的核心。它会：

- 从当前节点出发；
- 每一跳只读取当前节点的一跳邻居；
- 结合 `visited` 和动作 mask 过滤掉不该走的邻居；
- 在剩余邻居里做下一跳选择；
- 如果没有合法动作就记为 `loop_blocked`；
- 如果超过最大跳数就记为 `ttl_exceeded`。

#### 输入输出

- 输入：时间片、源节点、目的节点、当前队列、当前链路已用速率、AoI、配置、策略
- 输出：
  - `path`
  - `status`
  - `invalidTotal`
  - `candidateTotal`

#### 对应论文思路

这是最直接对应文稿里“逐跳本地决策 + 防环 + TTL”的代码块。

#### 我的写法思路

这一块是为了把“老师要求后续不要再深挖 Dijkstra，而要转向逐跳本地决策”真正落到代码上。也就是说，后面接 MARL 时，环境执行逻辑已经不是全局路径搜索，而是局部一跳决策了。

---

## 二、`leo_marl_env.py` 里的关键代码块

### 1. `SCENARIOS` / `ScenarioConfig`

#### 做什么

定义不同实验场景，比如：

- `low_load`
- `medium_load`
- `hotspot_high_load`
- `frequent_break`
- `fault_links`

每个场景会控制背景流量、热点流量、故障链路比例、可靠性惩罚、缩短 `t_rem` 的比例等。

#### 输入输出

- 输入：场景名称
- 输出：该场景对应的配置对象

#### 对应论文思路

直接对应文稿里的实验场景设计。

#### 我的写法思路

我把场景单独写成配置，是为了让后面统一评测和正式训练都能复用，不需要在脚本里手工切参数。

---

### 2. `_make_obs()`

#### 做什么

这是环境里最核心的观测构造函数。它会生成当前卫星的一跳邻居观测，包括：

- 当前节点队列；
- 邻居队列；
- 单跳时延；
- 剩余带宽；
- 链路负载；
- 链路可靠性；
- 剩余寿命；
- AoI；
- 进展值；
- `(U,W)` 轨道-测地特征占位。

#### 输入输出

- 输入：当前环境内部状态
- 输出：`obs` 字典

#### 对应论文思路

它对应文稿里的局部观测 `o_i` / `z_ij`。

#### 我的写法思路

我把观测统一整理成 `neighbor_features`，是为了后面不管接 cleanmarl、官方 MAPPO，还是别的库，都能直接把这些特征 flatten 或编码给 Actor 网络。

---

### 3. `_action_feasible()`

#### 做什么

这个函数负责实现动作掩码逻辑，判断某个候选邻居能不能选。当前过滤条件包括：

- 已访问节点不能再走；
- 链路不可用不能走；
- 剩余带宽不足不能走；
- 邻居队列爆满不能走；
- 可靠性低于阈值不能走；
- 剩余寿命低于阈值不能走。

#### 输入输出

- 输入：当前节点 `u`、候选邻居 `v`
- 输出：布尔值，表示动作是否可行

#### 对应论文思路

这直接对应文稿里的 action mask / 动作过滤规则。

#### 我的写法思路

这块我故意写成单独函数，是因为后面如果接正式 MAPPO，这个 mask 逻辑很可能还要继续迭代，所以必须让它和 reward、环境推进分开。

---

### 4. `_local_reward()`

#### 做什么

这是单跳局部即时奖励。它当前综合考虑：

- 时延代价；
- 邻居队列代价；
- 负载代价；
- 可靠性风险代价；
- 链路寿命代价；
- 目的方向进展奖励。

#### 输入输出

- 输入：当前节点、下一跳节点、当前边状态
- 输出：一个标量 reward

#### 对应论文思路

对应文稿里的局部即时奖励 `r_local`。

#### 我的写法思路

我没有把 reward 写得特别复杂，而是先把文稿里最核心的项落进去。这样后面做正式 MAPPO 时，可以继续调权重，而不用重写 reward 结构。

---

### 5. `as_mappo_inputs()`

#### 做什么

这个函数负责把当前环境观测整理成更接近 MAPPO / cleanmarl 使用的数据格式，输出：

- `actor_obs`
- `critic_state`
- `action_mask`

#### 输入输出

- 输入：当前观测 `obs`
- 输出：适配后的输入字典

#### 对应论文思路

对应文稿里的“执行时局部 Actor、训练时集中式 Critic”这条 CTDE 路线。

#### 我的写法思路

我单独写这个函数，是为了把环境逻辑和算法接口逻辑解耦。后面不管接 cleanmarl 还是官方 MAPPO，都能先从这个函数拿到统一输入。

---

## 三、`run_python_experiments.py` 里的关键代码块

### 1. `choose_action()`

#### 做什么

给不同策略统一提供动作选择逻辑。当前支持：

- `random_feasible`
- `delay_only`
- `queue_load`
- `full_masked_heuristic`
- `linear_policy`

#### 输入输出

- 输入：环境、当前观测、策略名
- 输出：动作索引

#### 对应论文思路

它对应论文里的“不同策略统一比较”。

#### 我的写法思路

我没有把每个策略分成独立脚本，而是统一放在一个选择函数里，这样后面新加 `mappo_policy` 时最方便，也最利于统一评测。

---

### 2. `run_episode()`

#### 做什么

负责在某个场景、某个策略下完整跑一个 episode，并记录：

- 是否送达；
- 是否丢包；
- 时延；
- 跳数；
- reward；
- 控制开销；
- FLOPs；
- 终止状态。

#### 输入输出

- 输入：场景名、策略名、种子偏移
- 输出：`EpisodeResult`

#### 对应论文思路

对应文稿里的单次实验执行和指标统计。

#### 我的写法思路

我把每个 episode 的结果都统一写进一个 dataclass，是为了后面聚合方便，避免临时拼字典导致字段乱掉。

---

### 3. `aggregate()`

#### 做什么

把多次 episode 的结果按场景和策略汇总成最终表格，计算：

- `deliveryRatio`
- `dropRate`
- `avgDelayMs`
- `p95DelayMs`
- `avgHops`
- `avgReward`
- `avgControlOverheadBytes`
- `avgControlOverheadRatio`
- `avgDecisionFLOPs`

#### 输入输出

- 输入：`EpisodeResult` 列表
- 输出：汇总后的行列表

#### 对应论文思路

它直接对应论文里最终要展示的表格指标。

#### 我的写法思路

这个函数的目的就是先把指标口径固定下来。这样后面不管接入什么学习算法，最后都走这里导出 CSV，保证对比口径一致。

---

## 四、`cleanmarl_leo_wrapper.py` 里的关键代码块

### 1. `reset()` / `step()`

#### 做什么

这两个函数把当前环境包装成 cleanmarl 能接受的形式。

#### 输入输出

- `reset()` 输出：`obs, info`
- `step(actions)` 输出：`next_obs, reward, done, truncated, infos`

#### 对应论文思路

它不是论文核心算法本身，而是“让当前环境能被正式 MAPPO 训练代码调用”的桥接层。

#### 我的写法思路

思路是先不动 cleanmarl 的训练逻辑，而是把我们自己的环境改成它能识别的样子。这样风险最低。

---

### 2. `get_avail_actions()`

#### 做什么

把当前环境的 `action_mask` 转成 cleanmarl 需要的可行动作矩阵格式。

#### 输入输出

- 输入：当前 wrapper 内部状态
- 输出：`(n_agents, action_size)` 形状的矩阵

#### 对应论文思路

对应 action mask / 动作过滤在训练接口里的落地形式。

#### 我的写法思路

cleanmarl 的 `mappo.py` 会在每一步直接取 `avail_actions`，所以这块必须明确单独做，不然 MAPPO 的动作采样就没法接 mask。

---

### 3. `get_state()`

#### 做什么

把集中式 Critic 需要的全局摘要整理成一个固定长度向量。

#### 输入输出

- 输入：当前环境状态
- 输出：固定长度 state 向量

#### 对应论文思路

对应文稿里的 CTDE / centralized critic state。

#### 我的写法思路

我这里没有直接把全网原始状态全塞进去，而是先用摘要向量，是因为当前目标是先把接口跑通。后面如果正式接 MAPPO，再按需要扩充 state 即可。

---

## 总结

从主要代码块来看，我现在已经完成了：

- 前期仿真主干；
- 环境观测与动作掩码主干；
- reward 主干；
- 统一评测主干；
- cleanmarl 风格桥接主干；
- 真实拓扑输入主干接口。

也就是说，当前项目已经不是“只有思路”，而是已经把论文里最重要的几个实现骨架都落出来了。后面真正要补的，主要就是：

- 正式 PyTorch MAPPO 训练；
- 真实星座数据真正接入；
- 更完整的论文级对比实验。
