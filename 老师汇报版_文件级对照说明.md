# 当前代码文件级对照说明（汇报版）

这份说明不是只讲“整体做了什么”，而是把当前项目里比较关键的文件一个个拆开，写清楚三件事：

1. 这个文件是做什么的；
2. 这个文件主要参考了哪些资料；
3. 我写这一块时的大概思路是什么，为什么这么做。

这样老师在看代码时，可以更容易判断：哪些是前期仿真，哪些是环境接口，哪些是训练桥接，哪些是为后面正式 MAPPO/真实星座做准备。

---

## 1. `run_preliminary_leo_routing.m`

### 作用

这是 MATLAB 前期仿真主脚本。它主要负责把论文里的前期验证思路先落到可运行代码里。当前它实现了：

- Dijkstra 最短路 baseline；
- queue/load-aware baseline；
- 链路寿命和可靠性动作掩码；
- 逐跳本地策略；
- `visited` 防环；
- `TTL` 限制；
- 局部 reward；
- 消融策略：
  - 去掉队列/负载；
  - 去掉链路寿命 mask；
  - 去掉可靠性风险；
  - 去掉目的方向进展。

它的用途不是做最终训练，而是支撑“前期问题验证”和“为什么后面要做 MARL”的论证。

### 主要参考资料

1. 我的研究文稿本身
   - 里面定义了时变图建模、局部观测、动作 mask、逐跳本地决策和消融实验思路。

2. 传统最短路 / Dijkstra baseline 思路
   - 作为全局最短路对照，用来说明只看传播时延的局限性。

3. 队列感知和链路寿命约束的研究思路
   - 来自我的文稿主线，而不是单独抄某一篇代码。

### 写这一块时的大概思路

我写这个 MATLAB 文件时，思路是先不要急着上 MAPPO，而是先回答几个基础问题：

- 只用 Dijkstra 会不会忽略队列和负载？
- 加上队列和负载以后，最大队列和 P95 时延会不会变好？
- 如果加上 `T_rem_ij` 动作掩码，能不能减少选到快断链路？
- 如果把路由改成逐跳本地决策，会不会更接近后面 MARL 的执行方式？

所以这个脚本的定位就是“前期仿真与消融”，先把论文里的问题动机和基础判断验证出来。

---

## 2. `leo_marl_env.py`

### 作用

这是后续接 MAPPO/CTDE 的 Python 环境层，不是新的独立算法。它现在负责：

- 场景配置 `SCENARIOS`；
- 局部观测 `neighbor_features`；
- `action_mask`；
- `visited` 防环；
- `TTL`；
- 局部即时奖励 `_local_reward()`；
- 集中式 Critic 需要的 `global_state_for_critic`；
- `as_mappo_inputs()` 适配输出；
- AoI / Hello 字段与控制开销；
- 控制预算约束；
- `topology_provider` 真实星座输入接口；
- `orbital_geodetic_coord()` 特征占位。

它是整个 Python 训练线的核心。

### 主要参考资料

1. 我的研究文稿本身
   - 这是最核心的参考来源。
   - 文稿里关于状态、动作、动作掩码、AoI、控制预算、局部观测、局部奖励、全局状态，都直接体现在这个文件里。

2. SHORT 论文
   - 主要参考了“真实 LEO 网络不能只看扁平全局路由”“每跳决策要尽量低开销”“可以引入 shell/orbit/geographic 特征”这些思想。
   - 对应代码里主要体现在：
     - `orbital_geodetic_coord()` 占位；
     - `shell_id`；
     - `topology_provider` 真实拓扑接口。

3. CleanMARL / MAPPO 的环境接口思路
   - 不是直接照搬算法，而是参考“环境需要提供什么给 Actor / Critic”。

### 写这一块时的大概思路

我写这个文件时的思路是：把论文里的“实时 Dec-POMDP + CTDE/MAPPO”从文字变成环境接口。

也就是说，我不是先去写 Actor 网络，而是先把后面训练一定会用到的东西整理出来：

- 当前卫星到底能看到什么；
- 哪些动作能选，哪些动作必须 mask 掉；
- reward 应该由哪些项组成；
- Critic 到底应该看到什么全局摘要；
- AoI 和控制开销怎么留接口；
- 真实星座未来从哪里接进来。

这样后面不管接哪个算法库，环境层都不会重写。

---

## 3. `run_python_experiments.py`

### 作用

这是统一评测入口。它的作用是固定一套“多场景、多策略、统一指标输出”的流程。当前能做的是：

- 跑 `low_load`、`medium_load`、`hotspot_high_load`、`frequent_break`、`fault_links` 五个场景；
- 对比 `random_feasible`、`delay_only`、`queue_load`、`full_masked_heuristic`、`linear_policy`；
- 输出 `python_policy_eval_metrics.csv`；
- 指标包括投递率、丢包率、平均时延、P95、跳数、reward、控制开销和 FLOPs。

### 主要参考资料

1. 我的研究文稿
   - 文稿里明确提出了实验场景、评价指标和对比方法需求。

2. 统一评测的工程需求
   - 不管后面是启发式还是学习型策略，都应该走同一套评测逻辑，避免结果不可比。

### 写这一块时的大概思路

我写这个文件时的思路很明确：后面不能每接一个新策略就另写一套实验脚本，否则最后论文里很难公平对比。所以我先把评测流程固定下来：

- 环境固定；
- 场景固定；
- 指标固定；
- 输出 CSV 固定。

这样后面真正训练出 MAPPO 策略，只要接进这个文件里新增一个 `mappo_policy`，就能和已有 baseline 用同一套标准做对比。

---

## 4. `train_linear_policy.py`

### 作用

这是一个轻量学习型 baseline。当前环境没有 PyTorch，所以我先用 numpy 写了一个最简单的 masked linear softmax policy，并且打通了：

训练 -> 保存权重 -> 再统一评测

输出文件包括：

- `models/linear_policy_weights.npz`
- `outputs/linear_policy_training_log.csv`

### 主要参考资料

1. 我的研究文稿
   - 文稿要求最终要有学习型方法，而不仅是启发式对照。

2. 当前本地环境约束
   - 由于当前没有 torch，无法直接跑正式 MAPPO，所以先做一个可运行的学习型 baseline。

### 写这一块时的大概思路

我写这个文件时的思路不是“发明新算法”，而是先解决一个很实际的问题：

当前环境到底能不能支持学习型策略？

如果连一个最简单的带 mask 策略都训不起来，那后面正式接 MAPPO 风险会很大。所以我先用最轻的方法把闭环打通，证明：

- 环境不是只能跑启发式；
- 策略可以训练；
- 权重可以保存；
- 训练后的策略可以走统一评测入口。

---

## 5. `cleanmarl_leo_wrapper.py`

### 作用

这是当前项目和 `cleanmarl` 之间最关键的桥。它把 `LeoRoutingEnv` 包装成 cleanmarl `mappo.py` 需要的接口：

- `n_agents`
- `reset()`
- `step(actions)`
- `get_avail_actions()`
- `get_state()`
- `get_obs_size()`
- `get_state_size()`
- `get_action_size()`

目前先把任务包装成 `n_agents = 1` 的最小兼容形式。

### 主要参考资料

1. `F:\cleanmarl\cleanmarl\mappo.py`
   - 我实际读了这个文件，按它的环境接口要求写的 wrapper。

2. 当前 `leo_marl_env.py`
   - wrapper 是在不破坏当前环境逻辑的前提下，做 API 对齐。

### 写这一块时的大概思路

我写这个文件时的思路是：不要一上来直接去重写 cleanmarl 的训练逻辑，而是先把环境包装到它能吃下去的接口形式。

也就是说，我把“研究环境”先变成“算法仓库可调用环境”。这样后面有 torch 的时候，接 cleanmarl 就是自然延伸，而不是重新做一套工程。

---

## 6. `train_cleanmarl_style_stub.py`

### 作用

这个脚本不会真正训练 PyTorch MAPPO，但它会尽量模仿 `cleanmarl/mappo.py` 的 rollout 数据采集方式，验证：

- `obs`
- `actions`
- `log_prob`
- `reward`
- `states`
- `done`
- `avail_actions`

这些键的结构是不是已经合理。

输出是：

- `outputs/cleanmarl_rollout_preview.csv`

### 主要参考资料

1. `F:\cleanmarl\cleanmarl\mappo.py`
   - 我读了它的 rollout buffer 和 episode 采集逻辑。

### 写这一块时的大概思路

我写这个文件时的思路是：当前没有 torch，那我至少要先把最容易出错的 rollout 数据结构验证掉。这样以后真接 cleanmarl 时，问题不会卡在“环境输出格式不对”。

---

## 7. `real_topology_provider_example.py`

### 作用

这是一个真实/准真实星座拓扑 provider 返回格式模板。它主要负责说明：

`topology_provider(t, env)` 应该返回怎样的 `LinkState` 字典。

### 主要参考资料

1. 我的研究文稿
   - 文稿要求后面接真实或准真实星座拓扑。

2. SHORT 论文
   - 提醒我真实网络里控制开销和层次化拓扑都很重要，所以需要尽早把真实拓扑接口预留出来。

### 写这一块时的大概思路

我写这个文件时的思路是：先把接口格式定死，而不是现在就硬写 TLE 解析器。因为只要 provider 格式先统一，后面无论接什么外部数据源，都不用重构环境。

---

## 8. `hypatia_topology_provider_stub.py`

### 作用

这是一个 Hypatia 风格真实拓扑接入 stub。它演示了：

- 如何从时间片级 CSV 快照读取链路信息；
- 如何把这些链路映射成 `LinkState`；
- 如何最终变成 `topology_provider` 可调用对象。

同时还会生成：

- `outputs/hypatia_topology_demo.csv`

### 主要参考资料

1. SHORT 论文和真实星座实验需求
   - 让我明确后面必须从 toy topology 走向准真实拓扑。

2. 我的研究文稿
   - 文稿里已经明确了后续要接真实/准真实星座拓扑。

### 写这一块时的大概思路

我写这个文件时的思路是：不是现在就去实现完整 Hypatia 依赖，而是先把“Hypatia 风格数据以后怎么进环境”写成一个中间层模板。这样等拿到真实数据时，只要填读取逻辑，不用重设计环境。

---

## 9. `README_前期代码说明.md`

### 作用

这是当前项目最重要的总说明文件。它负责：

- 说明当前有哪些文件；
- 说明每个策略是什么；
- 说明 MATLAB 和 Python 的分工；
- 说明环境接口和文稿的对应关系；
- 说明 cleanmarl / on-policy / BenchMARL / SHORT / 真实拓扑模板这些东西在项目里各自扮演什么角色。

### 主要参考资料

1. 我的研究文稿
2. 当前项目里所有核心代码文件
3. SHORT 论文
4. cleanmarl / on-policy / BenchMARL 的仓库定位

### 写这一块时的大概思路

我写这个 README 的思路是：让老师或我自己以后打开文件夹时，不用先猜哪个文件做什么、哪部分对应论文哪一节，而是能直接从 README 里看到项目整体结构。

---

## 10. 其余说明文件

当前还补了多份说明文件，它们的作用分别是：

- `TRAINING_NEXT_STEPS.md`
  - 写当前训练路线怎么从轻量 baseline 过渡到正式 MARL 训练。

- `CLEANMARL_INTEGRATION_PLAN.md`
  - 写 cleanmarl 正式接入时的操作思路。

- `ON_POLICY_INTEGRATION_PLAN.md`
  - 写为什么官方 MAPPO 放在第二阶段升级。

- `RUN_CLEANMARL_LEO.md`
  - 写在有 PyTorch 环境时，最小 cleanmarl 训练命令模板。

- `REAL_TOPOLOGY_INTEGRATION_PLAN.md`
  - 写真实/准真实星座数据怎么接到当前环境。

- `REAL_TOPOLOGY_DATA_FORMAT.md`
  - 写真实拓扑 CSV 至少需要哪些字段。

- `PAPER_PROGRESS_CHECKLIST.md`
  - 写当前已经完成的部分、没完成的部分和后续优先级。

- `TMT_REPO_MERGE_PLAN.md`
  - 写如果后续找到 `tmt-123-bit/leo-routing` 仓库，应该怎么逐项并到当前项目里。

### 写这些说明文件时的大概思路

我的思路是：当前环境里还没真正跑正式 MAPPO，也还没接真实星座和 tmt 仓库代码，所以光有代码不够，必须把“后面怎么接、为什么这么接、哪些先做哪些后做”提前写清楚，不然后面很容易断线。

---

## 总结

如果按文件来看，当前项目已经完成了：

- 前期仿真与消融主脚本；
- MARL 环境接口；
- 统一评测入口；
- 轻量学习闭环；
- CleanMARL 接口桥接；
- 真实拓扑 provider 模板；
- 多份接入和推进说明文档。

现在还没完成的是：

- 正式 PyTorch MAPPO 训练；
- 真实星座数据真正接入；
- 官方 MAPPO / BenchMARL 的正式实验；
- `tmt-123-bit/leo-routing` 仓库内容真正并进来。

也就是说，当前代码已经从“只有想法”推进到了“研究原型 + 环境接口 + 评测闭环 + 仓库桥接 + 真实拓扑模板”的阶段。
