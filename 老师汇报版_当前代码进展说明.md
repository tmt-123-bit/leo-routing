# 当前代码进展说明（汇报版）

老师您好，我把目前这个低轨卫星分布式动态路由题目的代码进展整理如下。

## 一、目前已经完成的工作

### 1. MATLAB 前期仿真原型

我先完成了一个 MATLAB 前期仿真主脚本，用来把论文里的研究思路先落到可运行原型中，验证几个最基础的问题。当前已经实现的内容包括：

- 把 LEO 网络建成时变图 `G(t)=(V,E(t),X(t))`；
- 实现传统最短路基线 `P0_delay_only`；
- 实现加入队列和链路负载的 `P1_queue_load`；
- 实现加入链路寿命、带宽和可靠性动作掩码的 `P2_queue_load_lifetime_mask`；
- 实现真正的逐跳本地策略 `P3_local_full`，加入 `visited` 防环和 `TTL` 限制；
- 实现多组消融策略，用来分别去掉队列/负载、链路寿命约束、可靠性风险、目的方向进展，观察指标变化。

这一部分主要是为了验证：为什么只看 Dijkstra 不够，为什么后面要引入队列感知、链路寿命约束、动作掩码和逐跳本地决策。

### 2. Python MARL 环境接口

在 MATLAB 前期仿真基础上，我又整理了一个 Python 环境接口文件。这个文件不是新的独立算法，而是后续接 MAPPO / CTDE 的环境层。当前已经完成：

- `reset()` / `step()` 接口；
- 当前卫星的一跳邻居局部观测；
- `action_mask` 动作掩码；
- `visited` 防环；
- `TTL` 限制；
- 局部即时奖励；
- 集中式 Critic 需要的全局摘要状态；
- AoI / Hello 状态交换开销；
- 控制预算约束；
- 真实或准真实星座拓扑输入接口占位；
- SHORT 论文启发下的 `(U,W)` 轨道-测地特征占位。

这一部分的作用是：为后续 cleanmarl、官方 MAPPO 或其他成熟 MARL 仓库接入打好环境基础。

### 3. 统一评测入口

我还补了一个 Python 批量评测脚本，用来统一跑不同场景和不同策略，并导出论文里需要的指标。当前可以导出的指标包括：

- 投递成功率；
- 丢包率；
- 平均端到端时延；
- P95 时延；
- 平均跳数；
- 平均 reward；
- 控制开销比例；
- 决策 FLOPs。

它的作用是：后面不管是启发式策略，还是训练出来的学习型策略，最后都走同一套评测流程，这样结果更统一，也更方便做论文表格和图。

### 4. 轻量学习型 baseline

由于当前环境里没有 PyTorch，我先没有直接跑正式 MAPPO，而是补了一个轻量学习型 baseline。这个 baseline 用 numpy 实现了一个带 action mask 的线性 softmax policy，并且已经打通了：

学习型策略训练 -> 保存权重 -> 再接统一评测输出指标

它的作用不是替代最终 MAPPO，而是先证明：当前环境已经支持学习型策略闭环，不只是规则启发式。

### 5. 面向正式 MAPPO 的桥接工作

为了后面尽快接正式 MARL 仓库，我已经做了几件桥接工作：

- 读取并评估了 `cleanmarl`、官方 `on-policy` 和 `BenchMARL` 三个仓库；
- 按 cleanmarl 的 `mappo.py` 环境接口要求，写了 `cleanmarl_leo_wrapper.py`；
- 补了 rollout 数据结构验证脚本；
- 写了 cleanmarl 接入计划、官方 MAPPO 升级计划和训练路线说明；
- 写了 cleanmarl 环境注册模板和运行命令模板。

目前最适合优先接的是 `cleanmarl`，因为它最轻，环境接口要求也最直接。

### 6. 真实星座拓扑输入模板

虽然现在还没有真正接入 TLE / Hypatia / StarryNet 的真实数据，但我已经把接口和模板准备好了，包括：

- 真实拓扑 provider 返回格式模板；
- Hypatia 风格拓扑 provider stub；
- 真实拓扑 CSV 最小字段格式说明；
- 接入真实/准真实星座的步骤说明。

这部分的作用是：后续如果拿到真实或准真实拓扑数据，不需要重写环境，只需要把外部数据转成统一格式，再通过 provider 接进来就行。

---

## 二、这些代码当前的用途

我现在做的这些代码，主要不是最终投稿代码，而是论文前期原型和训练过渡层。

- MATLAB 部分：前期问题验证和消融实验；
- Python 环境部分：后续正式接 MAPPO/CTDE 的环境层；
- 统一评测脚本：保证所有策略走同一套指标导出流程；
- 轻量学习型 baseline：先打通学习型策略闭环；
- cleanmarl / MAPPO 桥接文件：为后续正式训练做准备；
- 真实拓扑模板：为后续升级到真实星座实验做准备。

---

## 三、目前参考过的材料来源

### 1. 研究文稿

这是最核心的参考来源，整个代码主线基本都按研究文稿在推进。像时变图建模、局部观测、逐跳本地决策、动作掩码、防环、TTL、AoI、控制预算、真实星座接口等，都是按文稿里的结构落代码的。

### 2. SHORT 论文

参考论文：

`Stable Hierarchical Routing for Operational LEO Networks`

这篇论文主要提供了四点启发：

- 真实 LEO 网络不能只看扁平全局路由；
- 控制开销和路由更新代价很关键；
- 可以引入 shell/orbit/geographic 分层视角；
- 每跳决策最好保持近 O(1)，实验指标也不能只看平均时延。

因此我在环境里预留了 `(U,W)` 轨道-测地坐标特征占位和真实拓扑 provider 接口。

### 3. cleanmarl

这是当前实际用得最多的外部算法仓库参考。我已经读取了它的 `mappo.py` 接口要求，并按它的环境 API 写了 wrapper 和 rollout stub。

### 4. 官方 MAPPO on-policy

这个目前主要作为后续正式 MAPPO 版本的升级方向参考，还没有真正接进去跑训练。

### 5. BenchMARL

这个目前主要作为 benchmark 框架结构参考，没有直接拿来接训练。

### 6. `tmt-123-bit/leo-routing`

这个仓库是老师给出的重要参考方向，但当前我还没有定位到它的本地仓库路径，也没有真正读到它的本地代码，所以目前只能把它作为研究笔记/设计方向参考，后续找到路径后再逐项并进当前项目。

---

## 四、目前还没完成的部分

目前还没有完成的主要有三块：

1. 还没有真正跑 cleanmarl / 官方 MAPPO 的正式 PyTorch 训练；
2. 还没有把真实星座数据真正接进环境；
3. 还没有完成最终 IEEE 级别的多种子、大规模、全对比实验。

所以现在最准确的说法应该是：

我已经完成了前期仿真原型、MARL 环境接口、统一评测入口、轻量学习闭环、算法仓库桥接和真实拓扑模板；下一步是在有 PyTorch 的环境里正式接 cleanmarl 或官方 MAPPO 跑训练，再接真实星座拓扑做更完整实验。
